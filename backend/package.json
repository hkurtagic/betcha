{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/*.js --glob",
    "start:dev": "nodemon backend/server.ts",
    "tsc": "tsc --project backend/tsconfig.json",
    "build": "npm run clean && tsc --project backend/tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "stop": "npx kill-port 3000",
    "production": "tsc --project server/tsconfig.json && node ./dist/server.js",
    "nodemon:production": "nodemon dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/socket.io": "^3.0.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
